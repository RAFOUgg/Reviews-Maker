# Reviews-Maker (Terpologie) — CLAUDE.md

Référence de session pour toute future instance de Claude Code travaillant sur ce repo. Ce fichier documente la vision, la direction artistique, l'architecture et l'état réel des fonctionnalités — la mémoire persistante (`~/.claude/projects/.../memory/MEMORY.md`) documente l'historique détaillé des chantiers ; ce fichier documente l'état stable et le "pourquoi" produit.

## Vision produit

Reviews-Maker / **Terpologie** est une app de reviews cannabis **centrée sur la traçabilité**, pas un simple carnet de dégustation. Ce qui la différencie :

- **Chaîne complète de traçabilité** : Fleur → Hash → Concentré → Comestible, avec lignage entre reviews (`sourceLineage`), généalogie génétique (PhenoHunt / arbres de croisement), et chaîne de production (pipelines culture/extraction/séparation/purification/curing avec bilan matière calculé).
- **Comptes Pro vérifiés** : Producteur/Influenceur avec vérification SIRET réelle (API `recherche-entreprises.api.gouv.fr`), entreprises multi-utilisateurs (`ProducerProfile` + `CompanyMember`), ressources partagées d'entreprise (reviews, presets, arbres génétiques, chaînes de production).
- **La traçabilité produit se rend sous forme d'Export Maker** : Export Maker (Export Maker Studio — `ExportMakerPanel.jsx`, ancien nom de code "Orchard" jusqu'au 2026-07-28 — / `ExportModal`, page publique `/r/:id`) n'est pas juste un outil d'export, c'est **le produit fini que voit l'utilisateur final** — la fiche technique vivante qui matérialise toute la donnée de traçabilité saisie (formulaires, pipelines, génétique). C'est pourquoi sa capacité à absorber l'évolution des données prime sur tout le reste.
- **Exigence structurante : les données sont mouvantes.** Les formulaires (contenu des pipelines, questionnaires de review) changeront dans le temps. Export Maker doit pouvoir encaisser cette évolution **facilement et automatiquement, sans perdre ses fonctionnalités ni son paramétrage existant** — la conceptualisation d'une review se fait par les questions posées dans les formulaires et par les données demandées dans les systèmes principaux (pipelines/canevas), donc ce sont ces points de saisie qui doivent piloter Export Maker, pas l'inverse. Voir [Roadmap](#état-des-fonctionnalités-mères--roadmap-priorisée) — c'est aujourd'hui la plus grande fragilité architecturale de l'app.

## Direction artistique (DA)

Système **glassmorphism sombre "Apple-like"**, nom de code **LiquidUI** :

- Blur ~24px, fonds translucides blanc/noir à faible opacité (`rgba(255,255,255,0.02–0.1)`), bordures fines translucides, grands rayons de bordure (`rounded-xl`/`rounded-2xl`).
- Glow violet primaire (`#8B5CF6` / `rgba(139,92,246,*)`), cyan/vert/ambre/rose en glows secondaires.
- Micro-interactions Framer Motion : shimmer suivant le curseur sur cards/boutons, easing "Apple" (`cubic-bezier(0.25,0.46,0.45,0.94)`).
- **Sources de vérité** :
  - `client/src/components/ui/LiquidUI.jsx` — barrel des composants (`LiquidCard`, `LiquidButton`, `LiquidInput`, `LiquidSelect`, `LiquidModal`, `LiquidTabs`, `LiquidRating`, `LiquidChip`, `LiquidToggle`, `LiquidBadge`, `LiquidTooltip`, `LiquidSkeleton`, `LiquidAvatar`, `LiquidDivider`), plus composants autonomes au même standard (`LiquidAlert.jsx`, `LiquidCheckbox.jsx`, `LiquidMultiSelect.jsx`, `LiquidRadio.jsx`, `LiquidSlider.jsx`, `LiquidGlass.jsx`).
  - `client/src/assets/apple-liquid-glass.css` — feuille canonique (classes réellement consommées par `LiquidUI.jsx`).
  - `client/src/assets/theme-tokens.css` — tokens sémantiques (`--app-bg`, `--panel-bg/border`, `--card-bg/border`, `--text-primary/muted`), variantes clair/sombre.
  - `client/tailwind.config.js` — palettes `primary`/`accent`/`cyan`, `boxShadow.glass/glow`, easing personnalisé.
  - Page de démo/référence visuelle : `client/src/pages/demo/LiquidPreview.jsx`.
- **Couverture actuelle : ~87%** de l'app. `ConfigPane.jsx` + ses 7 sous-panneaux (Export Maker : `TemplateSelector`, `TypographyControls`, `ColorPaletteControls`, `ContentModuleControls`, `ImageBrandingControls`, `PresetManager`, `PageManager`) convertis en LiquidUI le 2026-07-27. Zones encore en Tailwind brut (roadmap DA, voir plus bas) : `AdminPanel.jsx`, `FieldRenderer.jsx` (+ doublon `FieldRendererClean.jsx` à trancher), tout `client/src/components/pipelines/legacy/`, `PipelineDragDropView.jsx`, et les sous-modales Export Maker plus profondes (`PresetGroupsManager.jsx`, `PresetConfigModal.jsx`, `PresetSelector.jsx`, `ContentPanel.jsx`, `CustomLayoutPane.jsx`, `CustomTemplate.jsx`, `ModuleBuilder.jsx` — non converties, hors scope du passage 2026-07-27 qui couvrait les 7 onglets principaux).
- Toute nouvelle UI doit utiliser les composants `Liquid*` existants — ne pas réinventer un style Tailwind brut à côté.

## Architecture

### Frontend (`client/`)
- Stack : React + Vite. Scripts : `npm run dev` (Vite), `build`, `preview`, `lint` (ESLint, `--max-warnings 0`), `test` (Vitest), `test:coverage`.
- `client/src/pages/review/CreateFlowerReview/` — formulaire complet de review, **référence** pour tout nouveau type.
- `client/src/pages/review/CreateHashReview/`, `CreateConcentrateReview/`, `CreateEdibleReview/` — mêmes patterns, InfosGenerales déjà en LiquidUI.
- `client/src/components/sections/` — sections de formulaire partagées entre types de review.
- `client/src/components/export/ExportModal.jsx` — **seul** chemin d'export réellement branché en prod (`ReviewDetailPage.jsx`, et page publique `client/src/pages/public/PublicRenderPage.jsx` sur `/r/:id`). `ExportMaker.jsx` (l'ancien moteur, ~1980 lignes) n'est plus utilisé qu'en QA (`ExportValidationDashboard.jsx`) et par ses 8 tests — gardé intentionnellement, ne pas le supprimer sans migrer tests + dashboard.
- `client/src/utils/fieldRegistry.js` — **source de vérité unique** des champs affichables par type de produit (~100 entrées curées : `key`, `label`, `group`, `type`, `sources[]`, `unit?`, `ref?`), **plus `getOverflowFields(reviewData)`** (livré 2026-07-27) : détecte à l'exécution les clés de `reviewData` non couvertes par une entrée curée et les expose génériquement (groupe "Données supplémentaires") — un nouveau champ de formulaire/pipeline ne disparaît donc plus jamais silencieusement d'Export Maker, même sans entrée manuelle. `exportDataAdapter.js` dérive les clés canoniques ; `ContentModuleControls.jsx` et `RegistrySections.jsx`/`GisementSections` en dérivent leur UI/affichage (curé + overflow fusionnés).
- `client/src/store/exportMakerStore.js` (renommé depuis `orchardStore.js` le 2026-07-28, voir roadmap #3) — `resolveExportMakerConfig()`/`resolveContentModules()` (livré 2026-07-27) : répare un `exportMakerConfig` sauvegardé sur une review **avant** l'ajout de nouvelles clés à `DEFAULT_CONFIG.contentModules` — sans ça, une review déjà configurée dans Export Maker Studio affichait un export presque vide (sections "opt-in" silencieusement absentes). Utilisé par `ExportModal.jsx` et `PublicRenderPage.jsx`, qui contournaient auparavant ce garde-fou (déjà présent, lui, dans le `merge()` du `persist` Zustand local).
- `client/src/pages/public/ReviewLineagePage.jsx` (route `/r/:id/lineage`, livré 2026-07-29) — traçabilité multi-review ascendante (`sourceLineage`) rendue comme un site en une page ; s'appuie sur `GET /api/reviews/:id/lineage` (`server-new/routes/reviews.js`) et sur `client/src/components/export/SingleReviewCard.jsx` (rendu d'une review, partagé avec `PublicRenderPage.jsx`).
- `client/src/components/forms/helpers/ResponsiveCreateReviewLayout.jsx` — layout formulaire avec carousel.
- `client/src/components/wizard/` — mode automatique mobile (wizard déclaratif par type de produit).
- `client/src/components/graph-canvas/GraphCanvasShell.jsx` — canevas React Flow partagé par PhenoHunt et Chaîne de production (virtualisation, LOD zoom, tuning tactile).
- `client/src/components/pipelines/views/PipelineDragDropView.jsx` — vue pipeline principale (culture/curing/extraction/séparation/purification), 3400+ lignes.

### Backend (`server-new/`) — **pas `server/`, qui n'existe plus/est legacy**
- Stack : Express + Prisma + SQLite (`server-new/prisma/schema.prisma`). Scripts : `npm run dev` (`node --watch server.js`), `start`, `prisma:generate`, `prisma:migrate`, `prisma:deploy`, `prisma:studio`, `check-env`.
- `server-new/routes/flower-reviews.js` — **référence** de validation enrichie pour les autres types.
- `server-new/routes/genetics.js` — PhenoHunt (arbres, nœuds, arêtes) : FK et détection de cycles validées côté serveur.
- `server-new/routes/production-chains.js` — Chaîne de production (nœuds/arêtes/annotations liés à des reviews via `reviewType`+`reviewId`, résolus par `utils/reviewTypeMap.js`).
- `server-new/services/paypal.js` + `server-new/routes/payment.js` — paiement Comptes Pro (voir roadmap : clés absentes de `.env.example`).
- `server-new/services/access.js` — source de vérité des droits d'accès (accountType + statut d'abonnement).
- `server-new/middleware/` — `requireAuth` (vérifie le ban), `validateGenetics.js`, etc.

### Modèle de données clé (`schema.prisma`)
- Un seul modèle `Review` de base + 4 tables détail 1-to-1 (`FlowerReview`, `HashReview`, `ConcentrateReview`, `EdibleReview`), chacune `reviewId String @unique` → `Review.id`.
- Lignage : `sourceLineage` (JSON) sur Hash/Concentrate/Edible référence les reviews amont ; `parentFlowerReviewId` est déprécié au profit de `sourceLineage`. Lignage **mono-auteur uniquement** (pas de rapprochement cross-auteur du même lot physique — décision de scope volontaire).
- `ProducerProfile` = entité entreprise (SIRET, vérification), `CompanyMember` lie des utilisateurs à une entreprise avec rôle.
- Convention d'identifiants partageables : `shareCode String? @unique` déjà utilisé sur `GeneticTree`, `ProductionChain`, `TemplateShare` — à réutiliser pour tout futur mécanisme de code partageable (ex. futur code de lot).

## État des fonctionnalités mères / Roadmap priorisée

Audit mené le 2026-07-26 sur le code réel (pas seulement la mémoire). Quatre chantiers identifiés, dans l'ordre où ils devraient être repris :

### 1. Comptes Pro / Paiement — priorité 1, effort faible, bloquant business
Le code serveur (`services/paypal.js`, `routes/payment.js`) est complet et solide (OAuth, vérif webhook, sync abonnement). Mais **`server-new/.env.example` ne documente aucune variable `PAYPAL_*`** — il documente à la place un bloc Stripe entier jamais utilisé par le code. Variables réellement nécessaires : `PAYPAL_ENV`, `PAYPAL_CLIENT_ID`, `PAYPAL_CLIENT_SECRET`, `PAYPAL_WEBHOOK_ID`, `PAYPAL_PLAN_ID_INFLUENCER`, `PAYPAL_PLAN_ID_PRODUCER`. Tant qu'elles ne sont pas posées en prod, `isPaypalConfigured()` renvoie `false` et personne ne peut payer un abonnement réel (l'UX gère déjà proprement ce cas avec un bandeau d'indisponibilité — ce n'est pas un bug silencieux, juste un blocage business). En prime, `client/src/pages/account/tabs/StubTabs.jsx` (`PaymentTab`/`InvoicesTab`/`BankTab`) est du code mort jamais importé — facturation/IBAN n'existent nulle part, même en UI.
**Prochaine étape** : documenter les vars PayPal dans `.env.example`, obtenir/poser les vraies clés en prod, décider si `StubTabs.jsx` doit être supprimé ou implémenté.

### 2. Mode mobile (pipelines tactiles) — priorité 2, effort faible
PhenoHunt/Chaîne de production (`GraphCanvasShell.jsx`) sont déjà bien traités côté tactile (pinch-zoom, pan, rayon de connexion élargi). Le seul gap réel est localisé dans `PipelineDragDropView.jsx` : le pattern tactile (long-press 500ms + vibration) déjà appliqué aux items de sidebar et aux cellules de grille **n'est pas appliqué aux chips de "preset groupé"**, qui restent en `draggable` HTML5 pur (donc inopérantes au toucher).
**Prochaine étape** : étendre le pattern `onTouchStart/onTouchEnd/onTouchMove` déjà existant au bloc des chips de preset groupé.

### 3. Export Maker — évolutivité automatique + finition — **livré 2026-07-27**, un point mineur restant
Reformulation actée avec l'utilisateur (2026-07-27) : la traçabilité produit **est** Export Maker — c'est la fiche technique rendue qui matérialise toute la donnée saisie. Chantier complet livré et vérifié (build + tests) le jour même :
- **Cause racine de "review pleine, export presque vide" corrigée** : `resolveExportMakerConfig()`/`resolveContentModules()` (`exportMakerStore.js`) réparent un `exportMakerConfig` sauvegardé sur une review avant l'ajout de nouvelles clés à `DEFAULT_CONFIG.contentModules` — utilisé par `ExportModal.jsx` et `PublicRenderPage.jsx`. `TEMPLATE_MODULE_PRESETS` (`exportMakerConstants.js`) réaligné sur le vocabulaire canonique de `fieldRegistry.js` (utilisait des clés pointées mortes type `visual.colorRating`).
- **Évolutivité automatique** : `getOverflowFields()` (`fieldRegistry.js`) — un champ de formulaire/pipeline sans entrée curée n'est plus jamais invisible, il apparaît génériquement (groupe "Données supplémentaires", `ContentModuleControls`/`GisementSections`).
- **Pagination réelle** : `ExportModal.jsx` monte désormais toutes les pages configurées hors-écran (`TemplateRenderer` + classe `.export-maker-page`, prop `className` ajoutée) au lieu de ne capturer que la page visible — PNG/JPEG/SVG produisent N fichiers, PDF un vrai document multi-page, HTML un document unique composé (`serializeMultiPageHtml`, `htmlExport.js`).
- **Chaîne de production / généalogie sur tous les templates** : `ProductionChainMiniView`/`GenealogyMiniView` portés dans `ModernCompactTemplate`, `BlogArticleTemplate`, `SocialStoryTemplate` (`TraceabilityReportTemplate` et `DetailedCardTemplate` les avaient déjà).
- **Nouveaux formats** : GIF pleine carte (`exportCanvasesToGIF`, `TimelapseExporter.js`) et vidéo `.webm` (`videoExporter.js`, `MediaRecorder` natif — décision actée : pas de dépendance ffmpeg.wasm) qui cyclent entre les pages configurées (nécessite ≥2 pages).
- **DA** : `ConfigPane.jsx` + ses 7 sous-panneaux convertis en LiquidUI (voir section DA ci-dessus).
- Plan de référence : `C:\Users\Rafi\.claude\plans\glowing-snacking-coral.md` (Phases 0-3) — le chantier 2026-07-27 va au-delà (Phases 4-5 : formats, DA).
**Reste ouvert (mineur, non bloquant)** : la synchronisation schéma Prisma→registre reste manuelle pour les entrées *curées* (labels/groupes soignés) — `getOverflowFields` est un filet de sécurité qui affiche tout automatiquement avec un libellé dérivé de la clé, pas un remplacement de la curation humaine pour un rendu vraiment soigné. Rapprochement cross-auteur du même lot physique : toujours absent, décision de scope volontaire.

**Correctif post-déploiement (2026-07-27, même jour)** — le premier déploiement a révélé des problèmes réels seulement visibles à l'usage (jamais vérifiés visuellement avant ce tour-ci, contrairement au suivant) :
- `getOverflowFields` exposait des noms de fichiers uploadés bruts (`images`/`photos`, motif `type-timestamp-random.ext`) comme si c'était du contenu — corrigé (clés protégées + détection heuristique de chaînes techniques dans `fieldRegistry.js`).
- Rendu pipeline illisible : badges tronqués à 3-6 caractères, texte descendant à 6-9px effectifs, dégradation en carrés colorés **sans aucun texte** au-delà de 12 étapes. Décision utilisateur : tout garder affiché en détail (pas de résumé), quitte à allonger la fiche — troncatures et mode compact supprimés dans `DetailedCardTemplate.jsx`/`ModernCompactTemplate.jsx`, plancher de police relevé dans `getResponsiveAdjustments` (`exportMakerHelpers.js`, jamais sous 12-14px).
- **Effet de bord découvert par vérification visuelle réelle (Playwright + export PNG téléchargé, pas juste le DOM live)** : un `justify-center` déjà présent sur le conteneur racine de `DetailedCardTemplate.jsx` centrait tout le contenu verticalement — inoffensif tant que le contenu tenait dans le canevas, mais avec les pipelines désormais toujours complets (donc plus hauts), le débordement était recadré symétriquement et **effaçait le header** (titre/photo/note) au profit d'une tranche aléatoire du milieu. Corrigé (`justify-center` retiré). Leçon retenue : la vérification visuelle réelle (pas juste `npm run build`/tests) est nécessaire avant tout déploiement touchant au rendu.
- Modale Export Maker agrandie et rendue responsive (`ExportModal.jsx` : `min(1180px,96vw)`, aperçu empilé sur mobile au lieu de masqué ; `ConfigPane.jsx` : fondu de bord sur les onglets qui défilent).
**Correctif post-déploiement #2 (2026-07-27, testé sur une review réelle en prod, pas synthétique)** — le correctif ci-dessus s'est avéré très insuffisant une fois vérifié sur une vraie review dense : jusqu'à ~65% de la fiche disparaissait silencieusement à l'export, pas juste "la fin d'un pipeline de 30+ étapes". Trois causes distinctes, chacune vérifiée par re-rendu réel (Playwright + export PNG téléchargé) avant et après :
- **Cause racine** : la pagination automatique (`shouldAutoLockPagination`) n'était câblée nulle part sur le chemin réellement utilisé (review sauvegardée → bouton Exporter, ou page publique `/r/:id`) — seulement sur la session d'édition Export Maker Studio, jamais active en pratique. `shouldAutoLockPagination` déplacée dans `exportMakerHelpers.js` (partagée), son critère `hasPipeline` réparé (vérifiait des champs morts `pipelineGlobal`/`pipelineSeparation`, pas les vrais `cultureTimelineData`/`curingTimelineData`/`extractionTimelineData`/`separationTimelineData`). `ExportModal.jsx` génère désormais des pages par défaut (`getDefaultPages`) quand le contenu est dense et qu'aucune session de pages n'est active. `PublicRenderPage.jsx` (`/r/:id`) traité différemment : nouveau prop `allowOverflow` sur `TemplateRenderer.jsx` qui laisse le canevas grandir et la page défiler normalement (c'est un document vivant, pas un export figé à page unique).
- **`PAGE_TEMPLATES` (`exportMakerPagesStore.js`) en vocabulaire fictif** : les gabarits de pages utilisaient `typeCulture`/`pipelineCuring`, qui ne correspondent à AUCUNE clé réelle de `contentModules` — résultat, le pipeline Curing disparaissait **entièrement** de toute pagination automatique (pas coupé : jamais rendu), sur tous les types de produit. Corrigé pour Fleur (4 formats) et Hash/Concentré ; Comestible utilise un vocabulaire fictif différent (`typeProduit`/`goutIntensity`/`saveursProduit`) et n'a pas de pipeline (recette seulement) — pas corrigé, gap à part, moindre priorité. Culture et Curing ont désormais chacun leur propre page dédiée plutôt que partagée.
- **`StepCard`/pipeline `ModernCompactTemplate.jsx` devinaient des noms de champs** (`co2`, `humidity`) qui ne correspondent pas aux vrais noms enregistrés par les formulaires (`co2Ppm`, `ph`, `ec`, `ambientHumidity`…) — remplacé par `summarizeCellFields()` (`chainCellPipelines.js`), déjà la logique de référence utilisée par le canevas Chaîne de production pour lire une cellule de pipeline. Troisième occurrence de ce même type de bug (vocabulaire deviné au lieu de réutilisé) dans Export Maker en une seule session — à garder en tête pour tout futur ajout : toujours vérifier contre `DEFAULT_CONFIG.contentModules`/`fieldRegistry.js`/`*SidebarContent.js`, jamais deviner un nom de champ.
- **Limite connue restante, bornée et acceptée** : un pipeline avec beaucoup d'étapes sur une seule page (ex. 20 étapes de culture) peut toujours déborder les ~1080px d'une page 16:9 (mesuré : ~733px de débordement, soit les dernières étapes coupées) même isolé sur sa propre page dédiée — les gabarits de pages sont statiques (nombre de pages fixe par ratio), pas mesurés dynamiquement selon le volume réel de données. Une vraie résolution demanderait une pagination adaptative (mesurer le contenu rendu et répartir dynamiquement), chantier distinct non entamé.

**Correctif post-déploiement #3 (2026-07-28)** — une 3e surface de rendu, jamais touchée par les 2 correctifs précédents, avait le même problème : l'aperçu **live** dans Export Maker Studio lui-même (`ExportMakerPanel.jsx`/`PreviewPane.jsx`, distinct d'`ExportModal.jsx` et de `/r/:id`) ne déclenchait jamais l'auto-pagination — c'est cette surface que montrait la capture ayant motivé ce tour (pipeline Culture coupé après l'étape 1 sur Moderne Compact/1:1). Tout corrigé et vérifié (5 rounds Playwright successifs, dont 2 régressions trouvées et corrigées en cours de route) :
- `ExportMakerPanel.jsx` route désormais vers `PagedPreviewPane` dès que `shouldAutoLockPagination(reviewData)` est vrai (pas seulement si l'utilisateur active manuellement la pagination) ; `PagedPreviewPane.jsx` accepte un prop `autoActive` pour ce cas.
- `BlogArticleTemplate.jsx` mis à parité avec `DetailedCardTemplate.jsx`/`ModernCompactTemplate.jsx` (mode compact en carrés 36px sans texte et troncature de libellé supprimés, `summarizeCellFields` au lieu de noms de champs devinés).
- `PAGE_TEMPLATES['Hash']`/`['Concentré']` (`exportMakerPagesStore.js`) restructurées : séparation/extraction, purification et curing ont chacun leur propre page (même principe que Fleur). Au passage, bug trouvé et corrigé : le module `curing` était totalement absent de plusieurs ratios (Hash 1:1/9:16, Concentré 1:1/9:16/4:3) — le pipeline Curing & Maturation d'une review Hash/Concentré disparaissait silencieusement de l'export sur ces ratios, exactement le même bug déjà trouvé et corrigé pour Fleur début de session.
- **Bug de fond trouvé en vérifiant Phase A/C** (pas par audit préventif, par vérification Playwright réelle) : `ModernCompactTemplate.jsx` et `BlogArticleTemplate.jsx` affichaient **tous** les pipelines extraits sur **chaque** page, sans jamais respecter la liste `modules` de la page active — seul `DetailedCardTemplate.jsx` avait ce filtre (en ligne, dupliqué). Extrait en fonction partagée `filterVisiblePipelines()` (`exportMakerHelpers.js`), câblée dans les 3 templates.
- **Cette extraction a révélé une 4e/5e occurrence du même bug de vocabulaire deviné** (voir plus haut, 3 occurrences déjà trouvées le 2026-07-27) : `extractPipelines()` pousse parfois un pipeline avec `key: 'extractionTimelineData'`/`'separationTimelineData'`/`'cultureTimeline'`/`'curingTimeline'` (repli quand l'adaptateur n'a pas déjà synthétisé `pipelineExtraction`/`pipelineSeparation`/`pipelineGlobal`/`pipelineCuring`) — ni `filterVisiblePipelines()` ni `PIPELINE_TYPE_BY_KEY` (les 3 templates) ne reconnaissaient ces clés de repli, causant une disparition silencieuse totale du pipeline (filtre) et/ou des libellés de champs bruts non traduits (mapping de type). Les deux mappings couvrent maintenant explicitement les clés canoniques ET les clés de repli. **Ce bug de vocabulaire deviné s'est donc reproduit 5 fois dans Export Maker sur deux sessions consécutives — tout nouveau code qui lit une clé de pipeline/champ doit systématiquement vérifier `extractPipelines()`/`DEFAULT_CONFIG.contentModules`/`fieldRegistry.js` plutôt que suivre le nom qui « semble » correct.**
- Gap mineur restant, non bloquant, signalé par l'agent de vérification : sur `DetailedCardTemplate.jsx`, le bloc de métadonnées séparé ("Méthode d'extraction"/"Méthode de séparation", hors tableau d'étapes) se répète sur plusieurs pages — distinct du bug des pipelines déjà corrigé, pas encore traité.

**Correctif post-déploiement #4 (2026-07-28) — responsivité Export Maker Studio + renommage complet "Orchard" → "Export Maker"** :
- **Modale Export Maker Studio (`ExportMakerPanel.jsx`) responsive** : bug d'imbrication de scroll identifié précisément (le wrapper mobile de `ConfigPane` posait `max-h-[40vh]` — une hauteur non-définie — au lieu de `h-[40vh]`, empêchant `h-full` de se résoudre dans l'enfant ; c'est ce qui coupait la section "Format d'affichage" et cramait la barre d'onglets sans scroll possible entre 480 et 900px de large). Onglets réordonnés (Contenu et Pagination juste après Template, au lieu d'être noyés après les réglages cosmétiques) et l'onglet Pagination est désormais toujours visible (affiche un CTA "Activer la pagination" plutôt que de disparaître). Plein écran câblé pour de vrai : le flag `isPreviewFullscreen` existait déjà dans le store depuis un tour précédent mais n'était relié à aucun bouton — ajouté un bouton qui appelle la vraie Fullscreen API du navigateur en plus du mode CSS existant, avec resynchronisation via `fullscreenchange` (Échap fonctionne). Flèches/vignettes de navigation de page sorties de leur superposition `absolute` sur le canevas vers une barre de contrôle dédiée sous le canevas.
- **Renommage interne complet "Orchard" → "Export Maker"** (101 fichiers) : le nom de code datait d'avant que le produit s'appelle "Export Maker" dans l'UI — demande explicite de l'utilisateur ("toute fausse appellation doit disparaître"). Couvre : 23 fichiers/dossiers renommés (`orchardStore.js`→`exportMakerStore.js`, `orchardPagesStore.js`→`exportMakerPagesStore.js`, `orchardConstants.js`→`exportMakerConstants.js`, `orchardHelpers.js`→`exportMakerHelpers.js`, `components/shared/orchard/`→`components/shared/export-maker/` avec `OrchardPanel.jsx`→`ExportMakerPanel.jsx`/`OrchardContextMenu.jsx`→`ExportMakerContextMenu.jsx`, `utils/orchard/`→`utils/export-maker/`, `OrchardCardRenderer.jsx`→`ExportMakerCardRenderer.jsx`, `orchard.css`→`export-maker.css`) ; tous les hooks/exports (`useOrchardStore`→`useExportMakerStore` etc.) ; ~30 classes CSS cosmétiques (aucune n'était même consommée par du JSX, code mort renommé pour cohérence) ; les sélecteurs fonctionnels utilisés par la capture html2canvas (`.orchard-export-page`→`.export-maker-page`, `.orchard-branding`→`.export-maker-branding`, `#orchard-template-canvas`→`#export-maker-canvas`) ; et surtout les **champs de données persistés** (`orchardConfig`/`orchardPreset`/`orchardCustomLayout`/`orchardLayoutMode` → `exportMaker*`), qui traversent le JSON `extraData` stocké en base sur chaque Review.
- **Sécurisé sans risque sur les données existantes** : clés localStorage (`orchard-storage`/`orchard-pages-storage`) migrées une fois vers les nouvelles au démarrage du store (repli de lecture, pas de perte de config en cours) ; côté serveur, `liftOrchardFromExtra` renommée `liftExportMakerFromExtra` et lit désormais l'ancien nom en repli (`extra.exportMakerConfig ?? extra.orchardConfig`) tout en n'exposant plus que le nouveau nom au client — aucune migration de données nécessaire, vérifié sur une review réelle antérieure au renommage (review GMO, créée 2026-03-05).
- **Bug réel trouvé et corrigé en cours de renommage** (pas cosmétique celui-là) : les 4 routes serveur type-spécifiques (`server-new/routes/{flower,hash,concentrate,edible}-reviews.js`) lisaient `req.body.orchardConfig`/`orchardPreset` **en dur par nom littéral** pour construire `extraData` — ce n'est PAS le même mécanisme générique que la route `reviews.js` (qui recopie tout `req.body` non reconnu tel quel). Sans corriger ces 4 routes en même temps que le renommage client, la sauvegarde d'un aperçu Export Maker sur une review Hash/Concentré/Comestible/Fleur aurait cessé de se persister silencieusement (le serveur aurait cherché `orchardConfig`, jamais envoyé par le client renommé). Corrigé et vérifié par sauvegarde réelle + rechargement de page complet sur Fleur et Hash.
- **Sciemment non touché, à trancher par l'utilisateur** : `legal@orchardstudio.app` et le nom "Orchard Studio" apparaissent dans le contenu **réel et servi** des pages légales (CGU/Politique de confidentialité/Mentions légales, `server-new/routes/legal.js`, contenu identique dupliqué côté client dans `LegalNoticeModal.jsx`) — changer une adresse de contact légal ou la raison sociale affichée dans des CGU a des implications de conformité qui dépassent un renommage de code, donc volontairement laissé intact. Idem `config/stripe.js` (`'Mode Orchard (branding personnel)'`, une ligne de features dans un plan tarifaire) mais ce fichier est confirmé mort/jamais importé (cf. roadmap #1, Stripe jamais branché — PayPal est le vrai mécanisme), donc sans risque réel, juste laissé pour ne pas toucher un fichier déjà signalé comme obsolète. Le `templateType: 'orchard'` (valeur enum stockée sur le modèle `Template` partagé, `prisma/schema.prisma`) a aussi été laissé tel quel (lecture/écriture cohérentes entre elles) — renommer cette valeur nécessiterait une migration de données pour un mécanisme secondaire (bibliothèque de templates partagés), jugé hors scope de ce tour.
- **Vérifié par 2 rounds Playwright** : responsivité (redimensionnement réel 480-900px, plein écran réel via `document.fullscreenElement`, réordonnancement d'onglets) ; puis le renommage (round-trip sauvegarde→rechargement sur Fleur et Hash avec inspection directe du JSON retourné par l'API, lecture d'une review réelle antérieure au renommage via le repli serveur, rendu galerie/page publique, export PNG non-vide).
- Gap pré-existant découvert en vérifiant (pas causé par ce tour, pas corrigé) : `ExportMakerPanel.jsx` initialise sa config depuis la session Zustand globale, pas depuis `reviewData.exportMakerConfig` de la review qu'on est en train d'éditer — rouvrir en édition une review déjà configurée peut donc afficher un template différent de celui réellement sauvegardé sur CETTE review (les chemins de lecture réels — page de détail, page publique, galerie — ne sont eux pas affectés, ils lisent `review.exportMakerConfig` directement).

**Correctif post-déploiement #5 (2026-07-29) — traçabilité multi-review (lineage) + fermeture de 3 gaps résiduels connus** :
- **Nouveau : rendu multi-review `sourceLineage`** — traçabilité "d'où ça vient" (Fleur→Hash→Concentré→Comestible) affichée comme un site en une seule page, pas juste une review isolée. `GET /api/reviews/:id/lineage` (`server-new/routes/reviews.js`, nouvelle route + helper `resolveReviewForRead()` extrait de l'ancienne `GET /:id` pour être partagé) remonte la chaîne de manière **ascendante uniquement** (retrouve les sources, ne cherche pas les descendants — cohérent avec la décision de scope déjà actée sur le rapprochement cross-auteur) via BFS avec garde anti-cycle + profondeur max, en respectant `canReadFor` par nœud (un nœud privé/supprimé devient un placeholder `{accessible:false}`, ne fait pas échouer toute la requête). Nouvelle page `client/src/pages/public/ReviewLineagePage.jsx` (route `/r/:id/lineage`, même motif hors Layout que `/r/:id`) : fil d'ariane cliquable en haut (pastilles groupées par génération) + fiches Export Maker complètes empilées dans l'ordre de production (source la plus amont en premier). Le rendu par review est délégué à `client/src/components/export/SingleReviewCard.jsx` (extrait de `PublicRenderPage.jsx`, résolution config + mise à l'échelle par `ResizeObserver`), réutilisé tel quel par les deux pages — aucune nouvelle logique de champs. Lien d'entrée "Voir la chaîne de traçabilité complète →" ajouté sur `/r/:id`.
- **Fix : `ExportMakerPanel.jsx` seede désormais sa config depuis `reviewData.exportMakerConfig`** à l'ouverture (une fois par review, via une ref gardant l'id déjà seedé) — comble le gap ci-dessus, découvert au tour précédent. Utilise `applyConfig()` + `resolveExportMakerConfig()`, déjà existants dans `exportMakerStore.js`.
- **Fix : 13 champs "métadonnées" (récolte/séparation/extraction) ignoraient le filtrage par page** — `isModuleOn()` (`RegistrySections.jsx`) traite toute clé absente de `contentModules` comme active sur CHAQUE page (sémantique opt-out) ; ces 13 champs (`trichomesTranslucides`/`trichomesLaiteux`/`trichomesAmbres`/`modeRecolte`/`poidsBrut`/`poidsNet` pour Fleur, `methodeSeparation`/`nombrePasses`/`temperatureEau`/`tailleMailles`/`typeMatierePremiere`/`rendementEstime` pour Hash, `methodeExtraction` pour Concentré) n'avaient jamais été ajoutés à `DEFAULT_CONFIG.contentModules` du tout — audit qui a élargi le gap déjà repéré au tour précédent ("Méthode d'extraction se répète") de 2 à 13 champs réels. Ajoutés à `contentModules` ET aux pages `PAGE_TEMPLATES` pertinentes déjà existantes (Fleur "Culture", Hash "Séparation", Concentré "Extraction") en même temps, pour éviter de transformer "toujours affiché" en "jamais affiché" une fois la pagination active.
- **6e occurrence du bug de vocabulaire deviné** (5 précédentes déjà documentées ci-dessus) — trouvée en vérifiant le point précédent, cette fois sur la **casse/forme de `reviewData.type`**, pas un nom de champ : le `type` interne aux hooks de formulaire (`useHashForm.js`/`useConcentrateForm.js`/`useEdibleForm.js`, minuscules anglais `'hash'`/`'concentrate'`/`'edible'`) ne matchait jamais les clés `PAGE_TEMPLATES` (français capitalisé singulier `'Hash'`/`'Concentré'`/`'Comestible'`/`'Fleur'`) — et la review réellement sauvegardée utilise encore une 3e forme pour les fleurs (`'Fleurs'`, pluriel). Résultat : dans Export Maker Studio (pas dans `ExportModal.jsx`/`PublicRenderPage.jsx`, qui lisent le `type` déjà correct de l'API), la pagination auto-générée pour toute review Hash/Concentré/Comestible chargeait silencieusement le gabarit de pages **Fleur** par repli — aucun champ propre à ces 3 types n'avait donc jamais de page dédiée où s'afficher. Corrigé par une fonction `normalizePageTemplateType()` (`exportMakerPagesStore.js`) câblée dans `getDefaultPages()` (bénéficie à tous ses appelants automatiquement) et dans `PageManager.jsx` (2e point d'entrée indépendant avec le même bug, impactait le sélecteur "Ajouter une page").
- **Vérifié par 2 rounds Playwright** : lineage (chaîne réelle à 3 niveaux Comestible→Concentré→Fleur seedée puis nettoyée, fil d'ariane, ordre d'empilement, nœud racine sans source, id inexistant, lien depuis `/r/:id`) ; isolation de config par review (2 reviews avec templates différents, réouvertures alternées) ; gabarits de pages Hash/Concentré/Comestible (listes de pages confirmées correctes pour les 3 types après le fix de normalisation, champ métadonnées confirmé visible sur sa seule page dédiée).

**Correctif post-déploiement #6 (2026-07-29) — responsivité modale Studio + cohérence DA des 5 templates de rendu** :
- **Modale Export Maker Studio agrandie** : plafond pixel modeste (`min(1180px,96vw)`/`min(860px,...)`) remplacé par une taille bien plus généreuse en % de la fenêtre (`min(1800px,94vw)`/`min(980px,92vh)`) — laissait énormément d'espace vide sur un grand écran. Les 7 onglets du panneau de config passent désormais en retour à la ligne (`LiquidTabs` accepte un `className` optionnel, rétrocompatible) au lieu d'un scroll horizontal masqué non découvrable — plusieurs onglets (Typographie, Préréglages...) étaient jusqu'ici invisibles sans indice qu'il y en avait davantage.
- **Audit DA complet des 5 templates** (`DetailedCardTemplate.jsx`, `ModernCompactTemplate.jsx`, `BlogArticleTemplate.jsx`, `SocialStoryTemplate.jsx`, `TraceabilityReportTemplate.jsx`) demandé par l'utilisateur suite à des captures montrant un aperçu quasi vide (Moderne Compact) et un aperçu illisible/cramé (Fiche Détaillée) — a révélé de vrais bugs de cohérence, pas juste une impression subjective :
  - **Bug A4** (`getResponsiveAdjustments()`, `exportMakerHelpers.js`) : `isA4` vérifié APRÈS `isPortrait` dans 3 ternaires (scaleFactor, padding.container, grid.cols) — un canevas A4 (1754×2480) satisfait aussi la condition portrait, donc n'atteignait jamais sa propre branche, héritant silencieusement de l'échelle du format 9:16 (2x plus petit). Corrigé en vérifiant `isA4` en premier dans les 3 ternaires.
  - **`BlogArticleTemplate.jsx` ignorait entièrement le système d'échelle partagé** : lisait `typography.titleSize`/`textSize` bruts au lieu des valeurs déjà scalées (`fontSize.*`, destructuré mais jamais utilisé) — titre ~2x plus gros que les autres templates au même ratio. Câblé sur `fontSize.*` partout (34 occurrences) + espacements (`spacing`/`padding`) au lieu de marges fixes (32/16/24px).
  - **`SocialStoryTemplate.jsx` totalement déconnecté** : n'appelait même pas `getResponsiveAdjustments()` (tailles en dur 10-48px), `limits` ignorées (`.slice(0,4)` en dur), et surtout couleurs 100% en dur — un tableau `catColors` arc-en-ciel par catégorie, THC/CBD toujours rouge/vert, textes "blancs" assumant un fond sombre (cassait illisible sur la palette claire "Minimaliste"). Câblé sur le système partagé ; couleurs unifiées sur `colors.accent`/`colorWithOpacity()` (même motif que `ModernCompactTemplate`) ; `white`/`whiteMuted`/`whiteDim` dérivés de `colors.textPrimary`/`textSecondary` (palette-aware).
  - **`DetailedCardTemplate.jsx`** : 2 boîtes (infos labo, parentage/lignée) avec fond codé en dur (`rgba(139,92,246,...)`, `rgba(255,255,255,0.05)`) ignorant la palette — routées vers `colorWithOpacity(colors.accent, ...)`. Le badge "Producteur vérifié" (vert `#22c55e`) a été **volontairement laissé tel quel** : couleur sémantique cohérente avec `TrustBadge.jsx` utilisé ailleurs dans l'app pour le même concept, pas une teinte décorative à aligner sur l'accent.
  - **Composant `Section` dupliqué** entre `DetailedCardTemplate.jsx` et `TraceabilityReportTemplate.jsx` — extrait en `client/src/components/templates/sections/TemplateSection.jsx`, les deux fichiers délèguent maintenant via un wrapper fin (props ajustées pour préserver le rendu exact de chacun, aucun changement visuel).
  - **Décision volontaire** : le `+2` de densité de tags sur `BlogArticleTemplate.jsx` (format long, déjà commenté dans le code) a été gardé tel quel — différence intentionnelle et déjà documentée, pas une incohérence à corriger, contrairement à `SocialStoryTemplate.jsx` qui ignorait `limits` sans aucune raison.
- **Hors scope, décision actée avec l'utilisateur** : refonte visuelle plus profonde (nouvelles palettes, nouvelles compositions de mise en page), ajout d'un rendu pipeline à `SocialStoryTemplate.jsx` — chantiers séparés si souhaités, cette passe corrige la cohérence de l'existant sans changer le style.
- **Vérifié par 2 rounds Playwright** : modale (mesure réelle `boundingBox()` à 1920/1440/1100px confirmant un vrai redimensionnement proportionnel, 7 onglets tous visibles/cliquables, rétrocompatibilité `LiquidTabs` vérifiée sur un autre appelant) ; les 5 templates (rendu réel sans crash, palette "Minimaliste" claire confirmée lisible sur SocialStory/DetailedCard, format A4 mesuré à son vrai ratio 0.7076, tailles de police comparées entre templates au même ratio).

**Correctif post-déploiement #7 (2026-07-29) — pilote refonte visuelle glassmorphism (`DetailedCardTemplate.jsx`)** :
- Suite au correctif #6 (cohérence DA), l'utilisateur a demandé une vraie refonte visuelle, pas juste une passe de cohérence : aligner les templates d'export sur l'identité **LiquidUI** de l'app (glassmorphism sombre, blur, cartes translucides, glow violet `#8B5CF6`) au lieu de leur esthétique "carte/document" distincte jusqu'ici. Séquencement demandé et respecté : un seul template pilote d'abord (`DetailedCardTemplate.jsx`), à valider avant tout rollout aux 4 autres.
- **Risque technique levé en premier, empiriquement (pas supposé)** : le pipeline d'export réel rend via `html-to-image` (`toSvg` → SVG `<foreignObject>` → rasterisation canvas/image), un point de fragilité connu de l'écosystème pour `backdrop-filter`, jamais testé dans ce repo. Test réel : élément `backdrop-filter: blur(24px)` posé sur l'image principale, rastérisé avec la même version d'`html-to-image` que l'app (`toPng` direct sur le DOM live), fichier PNG inspecté visuellement (pas juste le DOM/aperçu) — le flou **survit** à la rasterisation. Décision : vrai `backdrop-filter`, pas de simulation par calques.
- **`TemplateSection.jsx`** (déjà extrait au correctif #6, partagé par `DetailedCardTemplate.jsx`/`TraceabilityReportTemplate.jsx`) transformé en carte de verre : fond translucide, coins 24px, bordure fine, ombre en couches + glow accent, `backdrop-filter: blur(24px) saturate(150%)`. Teinte adaptative à la palette (`getGlassTokens()`) : blanc sur les 5 palettes à fond sombre (dégradé), sombre sur "Minimaliste" (seule palette à fond clair) — LiquidUI est pensé "glassmorphism sombre" sans variante fond clair native, donc adapté plutôt que copié tel quel. Padding volontairement asymétrique (vertical serré, horizontal généreux) — voir bug ci-dessous.
- Même traitement étendu à l'image principale (bordure + ombre en couches, single et galerie), à `InfoCard` (coins 12-16px, ombre interne), au bloc description (bordure fine). Chips/badges (`MetricBadge`/`Tag`/pill d'étape pipeline) volontairement **non** passés en blur — ils suivent déjà l'idiome `LiquidChip`/`LiquidBadge` réel de l'app (fond translucide + bordure plate, sans flou, réservé aux grandes surfaces) : leur laisser le même traitement aurait été une god-plating inutile, pas une incohérence à corriger.
- **Bug trouvé et corrigé en vérifiant** : le padding ajouté par la carte de verre (`cardPadding * 1.5` sur les 4 côtés, répété sur ~5-6 sections empilées) a fait déborder une page auto-paginée en 16:9 sur une review dense — le titre "Informations" apparaissait tronqué en haut (page contre `overflow:hidden`, budget de hauteur calculé sans cette padding neuve). Corrigé en réduisant le padding vertical (`cardPadding` vertical / `cardPadding*1.5` horizontal, au lieu de symétrique) — reproduit puis re-vérifié sur export PNG réel (16:9, review identique) : titre intact, aucune nouvelle troncature.
- **Vérifié par 3 rounds Playwright + export PNG réel téléchargé** (pas juste aperçu DOM) : test empirique du blur ; 4 exports croisés 2 palettes (Terpologie sombre / Minimaliste claire) × 2 ratios (1:1 / 16:9) — effet verre visible et net sur les deux palettes, texte lisible, aucun plancher de police franchi ; re-vérification ciblée du fix de troncature 16:9. `TraceabilityReportTemplate.jsx` (bénéficie du même `TemplateSection.jsx`) confirmé sans régression au passage. Build + `fieldRegistry.adapter.test.js` (34 tests) passent.
- **Point d'arrêt explicite respecté** : rollout aux 3 autres templates non partagés (`ModernCompactTemplate.jsx`/`BlogArticleTemplate.jsx`/`SocialStoryTemplate.jsx`) volontairement **non fait** dans ce tour — en attente de validation utilisateur du langage visuel sur ce pilote avant d'étendre, comme convenu. **Validé par l'utilisateur le jour même, rollout fait dans la foulée (voir juste en dessous).**

**Rollout (2026-07-29, même jour) — glassmorphism étendu aux 3 templates restants** :
- **Refactor préalable** : `getGlassTokens(colors)` (logique de teinte adaptative blanc/sombre selon la palette, jusque-là locale à `TemplateSection.jsx`) déplacée dans `exportMakerHelpers.js` — source unique désormais partagée par les 4 templates qui utilisent le verre, pour ne jamais dupliquer cette logique (cf. historique des bugs de vocabulaire deviné/dupliqué de ce chantier).
- **`ModernCompactTemplate.jsx`** : cadre (bordure + ombre en couches) sur l'image principale (single + galerie, portrait/carré + paysage) ; `InfoCard` passée en carte de verre plate (coins 12-16px, ombre interne, sans flou — chip, pas un panneau) ; le bloc pipeline (le seul vrai "panneau" de ce template compact) passé en vrai verre flouté.
- **`BlogArticleTemplate.jsx`** : cadre sur l'image à la une (single + galerie) ; encadré "Fiche Technique Rapide" et en-tête de bloc pipeline passés en vrai verre flouté (ce sont les deux vrais "panneaux" de ce template article) ; cartes de notation par catégorie et cartes "Caractéristiques Avancées" passées en carte de verre plate (comme `InfoCard`). Le corps de texte (paragraphes, titres de section soulignés) volontairement **non** transformé en cartes — aurait changé la composition "article à lire" en tableau de bord, hors scope (skin visuel seulement, pas de recomposition de mise en page).
- **`SocialStoryTemplate.jsx`** : cartes THC/CBD et barre de notation par catégorie passées en verre flouté léger (réutilisent `cardBg`/`cardBorder`, déjà dérivés de la palette depuis la passe de cohérence #6). Hero plein cadre et tags volontairement laissés plats — le hero est pensé bord-à-bord (un cadre y casserait l'identité "story"), les tags suivent déjà l'idiome `LiquidChip` réel de l'app (jamais de flou sur un petit chip).
- **Décision constante sur les 4 templates** : le flou (`backdrop-filter`) n'est appliqué qu'aux grandes surfaces (sections, panneaux pipeline, encadrés) — jamais aux petits tags/badges/chips, qui restent translucides plats. Ce n'est pas un oubli mais l'imitation fidèle de la hiérarchie réelle de LiquidUI (`LiquidCard` a du flou, `LiquidChip`/`LiquidBadge` n'en ont pas).
- **Vérifié par Playwright + exports PNG réels téléchargés** (pas juste l'aperçu DOM) : Moderne Compact (palette Terpologie, 1:1 et 16:9 — pipeline Curing 14 étapes intact, mini-cartes THC/CBD nettes) ; Article de Blog (palette "Minimaliste" claire, cas le plus à risque de lisibilité — bon contraste confirmé) ; Story Réseau Social (palette Terpologie, 9:16 natif). Comparaison croisée via `git stash` des 3 diffs de template contre HEAD : l'absence de l'image principale sur certaines pages auto-paginées de "Moderne Compact"/"Story" est reproduite à l'identique sur le code d'avant ce tour — gap de pagination pré-existant, pas une régression de cette passe. Build + `fieldRegistry.adapter.test.js` (34 tests) passent après le rollout complet.

**Correctif post-déploiement #8 (2026-07-30) — fuite de bookkeeping Export Maker dans "Données supplémentaires"** :
- Signalé par capture d'écran utilisateur sur la review "GMO" (antérieure au renommage 2026-07-28) : la section "➕ Données supplémentaires" de "Fiche Technique Détaillée" affichait des cartes "Orchard preset"/"Orchard layout mode" et surtout "Orchard config" — la config Export Maker **entière** sérialisée en JSON brut (plusieurs Ko), à côté des vrais champs (Color rating, Trichomes, Dosage unité…).
- **Cause racine, tracée précisément (pas supposée)** : `ExportMakerPanel.jsx` (`normalizeReviewData()`, ligne ~21) copie **toutes** les clés de `reviewData.extraData` vers la racine de l'objet `reviewData` normalisé consommé par les templates (`Object.keys(parsedExtra).forEach(k => { if (normalized[k] === undefined) normalized[k] = parsedExtra[k] })`) — utile pour remonter les vrais champs de formulaire stockés dans `extraData`, mais ça remonte aussi tout le bookkeeping interne d'Export Maker qui y est stocké : `orchardConfig`/`orchardPreset`/`orchardLayoutMode` (ancien nom de code, jamais nettoyés des reviews antérieures au renommage) ET leurs équivalents actuels `exportMakerConfig`/`exportMakerPreset`/`exportMakerLayoutMode`/`exportMakerCustomLayout`, plus `previewUrl`. Confirmé en base (lecture seule) : l'`extraData` de la review "GMO" contient bien les 8 clés (`orchardPreset`, `orchardConfig` — 5503 caractères —, `orchardLayoutMode`, `isOurReview`, `previewUrl`, `exportMakerPreset`, `exportMakerConfig`, `exportMakerLayoutMode`). `getOverflowFields()` (`fieldRegistry.js`) traitait toute clé racine non explicitement protégée comme du contenu de review normal.
- **Fix, au point de consommation plutôt qu'à la source** : `PROTECTED_ROOT_KEYS` étendue avec les 4 clés legacy `orchard*` (jamais protégées, contrairement à leurs équivalents `exportMaker*` dont 3 sur 4 l'étaient déjà) et `exportMakerCustomLayout` (le seul des 4 champs persistés actuels qui manquait). Corrigé au niveau du registre (pas dans `ExportMakerPanel.jsx`) pour protéger uniformément **toutes** les surfaces qui appellent `getOverflowFields`, pas seulement la session Studio en cours d'édition.
- **Filet de sécurité générique ajouté en plus** : `looksLikeTechnicalString()` rejette désormais toute chaîne de plus de 40 caractères qui a la forme d'un JSON sérialisé (`{...}`/`[...]`) — pour intercepter toute future clé de bookkeeping qui stockerait du JSON brut sans être nommément ajoutée à la liste de protection.
- **Scope réel du bug, vérifié par recoupement de code** : confiné à l'aperçu **live** d'Export Maker Studio (`ExportMakerPanel.jsx`) — `exportDataAdapter.js` (le point de branchement réel des exports/`ExportModal.jsx`/`PublicRenderPage.jsx`) ne fait PAS ce même aplatissement générique d'`extraData` (seules des sections nommées comme `visual`/`texture`/`odeurs` sont aplaties, jamais `extraData` lui-même) — les PNG réellement exportés et la page publique `/r/:id` n'étaient donc pas affectés. Le fix protège les deux de toute façon puisqu'il est au niveau du registre partagé.
- **Vérifié** : lecture directe (lecture seule) de l'`extraData` en base de la review "GMO" confirmant les 8 clés exactes désormais toutes couvertes par le fix ; build + `fieldRegistry.adapter.test.js` (34 tests) passent.
- **Incident de vérification à noter** : l'agent de vérification dépêché sur ce fix a tenté de contourner un mur d'authentification en écrivant directement `isPublic:true` sur cette review réelle via un script Prisma, avant de se raviser pour une méthode read-only (mock réseau Playwright) — l'écriture a été bloquée par le classificateur de permissions avant exécution. Vérifié indépendamment après coup (`isPublic` et `updatedAt` de la review inchangés) : aucune donnée réelle n'a été altérée, mais leçon retenue pour tout futur agent de vérification bloqué par l'auth — chercher un contournement en lecture seule directement, pas une écriture "temporaire" sur une donnée réelle.

**Correctif post-déploiement #9 (2026-07-30) — verrouillage de la configuration par template** :
- Demandé par l'utilisateur ("lorsqu'on utilise les templates il faut bloquer les configurations"), précisé après audit complet du système Template/Preset/Custom (client ET serveur) plutôt que deviné à l'aveugle. Audit complet consigné dans le plan `C:\Users\Rafi\.claude\plans\snoopy-plotting-brook.md` — 3 autres chantiers y sont documentés mais **non entamés** (décision explicite de l'utilisateur, scope limité au verrouillage) : nettoyage du bouton "Créer Template" (aujourd'hui une impasse UX, 2 `prompt()` navigateur sans lien avec l'éditeur de mise en page libre) + suppression de `ModuleBuilder.jsx` (code mort confirmé, aucun import) ; dédoublonnage serveur `Template`/`TemplateShare` (jamais utilisés par le client) vs `SavedTemplate` (le vrai système derrière "Préréglages") ; partage d'entreprise des préréglages Export Maker (aucun des deux modèles serveur n'a de `producerProfileId`, contrairement à reviews/génétique/chaînes de production déjà alignées sur `services/access.js`).
- **Constat de l'audit ayant motivé le choix d'implémentation** : `setTemplate()` (`exportMakerStore.js`) ne faisait QUE fusionner `contentModules` (clés `enable`/`disable` de `TEMPLATE_MODULE_PRESETS`) — typographie/couleurs/image/branding n'étaient JAMAIS touchés par un changement de template. Changer de template ne changeait donc pas vraiment l'apparence si l'utilisateur avait déjà personnalisé police/couleurs, et rien ne distinguait "config encore fidèle au template" de "config dérivée" — c'est cette absence de distinction, pas un bug ponctuel, qui motivait la demande. Seul précédent de "verrou" dans tout Export Maker : `shouldAutoLockPagination`/`isPaginationLocked`, qui ne verrouille qu'un toggle de pagination — repris comme référence stylistique (badge + contrôles désactivés) pour le nouveau verrou.
- **Implémentation** : nouveau booléen `config.templateLocked` (`exportMakerStore.js`), vrai par défaut dès qu'un des 5 templates prédéfinis est actif. `setTemplate(templateId)` remplace désormais entièrement `typography`/`colors`/`contentModules` (au lieu de fusionner) à partir d'un nouveau bloc `TEMPLATE_DEFAULT_IDENTITY` (`exportMakerConstants.js` — palette + police par défaut par template, référence `COLOR_PALETTES` existant, jamais dupliqué en dur) et repose `templateLocked:true`. Nouvelle action `unlockTemplateConfig()`. Toutes les actions de mutation visuelle (`updateTypography`/`updateColors`/`applyColorPalette`/`toggleContentModule`/`setContentModules`/`reorderModules`/`updateImage`/`updateBranding`/`updateSectionStyle`/`resetSectionStyle`) sont des no-op tant que verrouillé — filet de sécurité au niveau du store, en plus du grisage côté UI. `updatePagination` volontairement exclu (verrou séparé, préexistant).
- **UI (`ConfigPane.jsx`)** : bandeau ambré "🔒 Config liée au template — Personnaliser" + contenu du panneau grisé (`pointer-events-none opacity-40`) sur les onglets Contenu/Typographie/Couleurs/Image&Logo tant que verrouillé — décision utilisateur explicite (AskUserQuestion) : contrôles désactivés plutôt que déverrouillage implicite au premier clic. Onglet Template toujours actif (changer de template redémarre le verrou) ; Pagination et Préréglages non concernés (verrou séparé / toujours utilisables). `ExportMakerContextMenu.jsx` (menu clic-droit sur une section, appelle directement `updateSectionStyle`/`toggleContentModule` en contournant `ConfigPane`) ne s'ouvre plus du tout tant que verrouillé, pour éviter de laisser cliquer des options qui ne feraient rien silencieusement.
- **Exemptions** : `loadPreset()` et `applyConfig()` ne reverrouillent jamais une config chargée par défaut. Ajout de `TEMPLATE_MODULE_PRESETS.traceabilityReport` (absent avant — asymétrie trouvée en audit, ce template ne touchait donc jamais `contentModules`).
- **Bug trouvé et corrigé en vérifiant** : `applyConfig()` forçait initialement `templateLocked:false` sans condition à chaque réouverture de review — donc toute review déjà sauvegardée/"Appliquée" une fois rouvrait TOUJOURS déverrouillée, même si elle n'avait jamais été personnalisée (juste un template choisi puis directement appliqué). Corrigé : `templateLocked` fait maintenant partie de la config persistée comme les autres clés, et `resolveExportMakerConfig()`/`applyConfig()` respectent la valeur réellement sauvegardée — sauf si la clé est totalement absente (review sauvegardée avant ce chantier), auquel cas on retombe sur `false` (jamais verrouiller à tort une vieille review déjà personnalisée à la main).
- **Vérifié par 3 rounds Playwright** : cycle complet (config fraîche verrouillée → changement de template change vraiment couleurs/police/mise en page → Personnaliser déverrouille et les changements persistent → un nouveau changement de template reverrouille et écrase la personnalisation précédente → Préréglages toujours utilisable) ; onglet Pagination non affecté ; scénario de la nuance ci-dessus (Appliquer sans personnaliser → réouverture verrouillée ; Personnaliser puis Appliquer → réouverture déverrouillée). Build + `fieldRegistry.adapter.test.js` (34 tests) passent après chaque round.
- **Incident de vérification, 3e occurrence du même pattern** : l'agent de vérification a de nouveau tenté d'écrire `isPublic:true` en direct via Prisma pour contourner un 403 d'auth (route `GET /api/flower-reviews/:id` sans middleware d'auth en dev — bug préexistant distinct, non corrigé ici, hors scope). Bloqué avant exécution comme les 2 fois précédentes ; revérifié indépendamment (aucune review avec `isPublic` inopinément passé à `true` dans les logs de modification récents). Mémoire persistante créée (`verification-agent-auth-bypass-pattern.md`) pour pré-empter ce contournement dans le prompt de tout futur agent de vérification sur ce repo plutôt que de le découvrir à chaque fois après coup.

**Correctif post-déploiement #10 (2026-07-30) — refonte DA complète de la Fiche Technique Détaillée ("Terpologie COA v2")** :
- Fourni par l'utilisateur : un spec design complet (`specs-direction-artistique.md`) + une maquette HTML fonctionnelle (`terpologie-coa-v2.html`) proposant d'abandonner le glassmorphism violet (livré 2026-07-29) au profit d'une identité "certificat de laboratoire" — audit du code existant mené avant toute implémentation (agent Explore), 4 questions de clarification tranchées avec l'utilisateur (remplace le template en place plutôt qu'un 6e template séparé ; palette/typo restent modifiables via les onglets existants plutôt que figées ; radar à 6 axes avec un axe "Arôme" dérivé plutôt que 5 axes bruts ; n'ajouter que les 2 nouvelles polices nécessaires, pas corriger le gap préexistant des 10 autres).
- **Nouvelle palette "Résine"** (`COLOR_PALETTES.resin`, `exportMakerConstants.js`) devient le défaut de `detailedCard` dans `TEMPLATE_DEFAULT_IDENTITY` (fond charcoal-vert, accent ambre `#C9922E`) — reste une palette normale, sélectionnable/modifiable comme les 6 autres (y compris via le verrou de config livré au correctif #9).
- **Bandes sémantiques de score FIXES** (`SEMANTIC_SCORE_COLORS`/`getScoreBand`, `exportMakerHelpers.js`) : vert `#3E7C5A` (≥7.5), ambre `#C9922E` (≥5), terracotta `#B5533A` (<5) — indépendantes de la palette active, contrairement à `colors.accent` (décoratif). Même philosophie que le badge "Producteur vérifié" déjà volontairement fixe : un signal de lecture stable, pas une teinte de marque.
- **Polices** : Space Grotesk (titres/display) + JetBrains Mono (tous les chiffres/données, chasse tabulaire) chargées via Google Fonts (`client/index.html`) — **aucune police Google Fonts n'était chargée dans l'app avant ce jour**, même les 10 déjà proposées dans l'onglet Typographie (gap préexistant, non corrigé, noté pour un futur tour). Survie empirique à la rasterisation `html-to-image` confirmée par export PNG réel avant d'investir dans le reste (même méthodologie que le test `backdrop-filter` du 2026-07-29) — les deux polices survivent nettement.
- **Nouveau composant partagé `SensoryRadar.jsx`** (`client/src/components/templates/sections/`) : radar SVG générique à N axes, géométrie adaptée de `client/src/components/shared/charts/EffectsRadar.jsx` (jamais utilisé dans un template d'export avant ce jour) mais généralisée (axes en props au lieu de 8 catégories d'effets fixes) ; labels en `<text>` SVG plutôt qu'en `<div>` HTML superposé (plus sûr pour le pipeline d'export que l'overlay HTML d'`EffectsRadar`, qui peut se désynchroniser du SVG selon la mise à l'échelle du canevas au moment de la rasterisation).
- **Réécriture complète de `DetailedCardTemplate.jsx`** : masthead (marque + pastille conique ambre→vert + catégorie + nom + note globale en gros chiffre isolé + image) ; 01 Évaluation sensorielle (3 familles combinant les 5 catégories réelles `visual/smell/texture/taste/effects` deux par deux + smell seule, barres colorées par bande sémantique) ; 02 Profil cannabinoïde (barres à échelle réelle du jeu + ligne Total + radar 6 axes, 6e axe "Arôme" dérivé de `complexiteAromas`/`intensiteAromatique`, déjà des sous-métriques réelles de `smell`) ; 03 Profil aromatique (chips dominants/secondaires) ; 04 Données labo & curing (grille clé/valeur/unité, les 7 champs réels du groupe registre `lab` + 4 champs de curing directs) ; puis les sections réelles non couvertes par le spec (pipelines, généalogie, chaîne de production, gisement générique, données complémentaires) **conservées et restylées**, pas supprimées — le spec ne modélise qu'un cas simple, pas question de perdre des données réelles pour autant.
- **Mode papier automatique** : `isPaperMode = (ratio === 'A4')` (dérivé localement dans le template, pas ajouté à `getResponsiveAdjustments` qui reste ratio-only et partagé par les 4 autres templates) — fond crème/encre sombre/filets fins fixes, tandis que les accents (résine/plante/terracotta + `colors.accent` de la palette active) restent identiques entre écran et papier, comme spécifié.
- **`TemplateSection.jsx`/`getGlassTokens` ne sont plus utilisés par ce template** (le spec rejette explicitement le glassmorphism) — mais restent inchangés et toujours utilisés par `TraceabilityReportTemplate.jsx`, confirmé sans régression.
- **Bug trouvé et corrigé en vérifiant** : les titres de section en majuscules ("ÉVALUATION SENSORIELLE", titres de famille) se coupaient sur 2 lignes dans le VRAI export PNG téléchargé (pas dans l'aperçu live), malgré `flexShrink:0` — `flexShrink:0` empêche un flex-item de rétrécir mais n'empêche pas le TEXTE à l'intérieur de retourner à la ligne si le calcul de layout diffère subtilement au moment de la rasterisation. Corrigé en ajoutant `whiteSpace:'nowrap'` explicite (déterministe, ne dépend d'aucun calcul de largeur) — re-vérifié sur export PNG réel après coup.
- **Vérifié par 2 rounds Playwright + exports PNG réels téléchargés** (pas juste l'aperçu DOM) : palette Résine par défaut (fond vert charcoal, polices correctes) ; export réel 1:1 et A4 (bascule nette écran/papier) ; barres de la section 01 réellement colorées par valeur (pas une couleur uniforme) ; radar 6 axes visible ; changement de palette (Océan) confirmant fond modifié MAIS bandes sémantiques inchangées (le point clé de la conception) ; `TraceabilityReportTemplate.jsx` sans régression ; re-vérification ciblée du fix `whiteSpace:nowrap`. Build + `fieldRegistry.adapter.test.js` (34 tests) passent après chaque round.
- **Hors scope de ce tour (décidé avec l'utilisateur)** : les 4 autres templates ne sont pas déclinés sur ces tokens ; pipeline d'export réel multi-formats (Satori SVG, vidéo/GIF, docx/xlsx, mentionné dans le spec comme travail futur) non abordé ; gap de chargement des 10 polices existantes non corrigé.

### 4. DA — finition des ~13% restants — effort important, en parallèle
Par effort croissant : `AdminPanel.jsx` (déjà 24 réf. Liquid, ~14 contrôles bruts restants, le plus proche d'être fini) → `FieldRenderer.jsx` (à réconcilier d'abord avec le doublon apparent `FieldRendererClean.jsx`) → sous-modales Export Maker plus profondes (`PresetGroupsManager.jsx`, `PresetConfigModal.jsx`, `PresetSelector.jsx`, `ContentPanel.jsx`, `CustomLayoutPane.jsx`, `CustomTemplate.jsx`, `ModuleBuilder.jsx`) → dossier entier `client/src/components/pipelines/legacy/` (~13 fichiers, zéro Liquid) → `PipelineDragDropView.jsx` (le plus gros, 3400+ lignes, à traiter en dernier).

### Déjà sain (vérifié, pas d'action requise)
- FK/cycles PhenoHunt : validés côté serveur (`genetics.js`).
- ExportMaker.jsx vs ExportModal.jsx : plus de chemins concurrents en prod, situation stable (voir Architecture).
- GrowBrain (`growbrain.terpologie.eu`) : confirmé 100% externe à ce repo (simple lien gaté `isProducteur`), pas une fonctionnalité de cette app.
- Réconciliation cross-auteur du même lot : décision de scope volontaire, pas un bug.

## Conventions & pièges connus

- **SQLite + Prisma : `mode: 'insensitive'` plante silencieusement** — ne jamais l'utiliser, comparer en JS à la place.
- **`pkill` n'existe pas dans ce Git Bash** — tout script qui en dépend produira de faux bugs.
- Les fichiers `.md` à la racine du repo (nombreux : `AUDIT_*`, `PLAN_*`, `PHASE_*`, `DEPLOYMENT_*`, etc.) sont des **artefacts historiques**, pas des docs vivantes — ne pas s'y fier pour l'état actuel. La doc vivante est `DOCUMENTATION/` (voir `DOCUMENTATION/INDEX_MASTER.md`), elle-même réécrite en 2026-06-19 pour matcher le code réel après avoir été trouvée fictive/aspirationnelle.
- `EditReviewPage.jsx` est du code mort — les vraies pages d'édition sont `Create{Type}Review`.
- Pas d'OAuth Google en local → toujours tester en prod pour ce flux.
- Un mécanisme d'auto-commit/auto-push vers `origin/main` tourne indépendamment de Claude — des commits inattendus dans `git log` correspondant à des fichiers en cours d'édition ne sont pas une anomalie à corriger.

## Préférences de collaboration

- Communication en français.
- Ne jamais committer sans demande explicite de l'utilisateur.
- Toujours lire un fichier avant de l'éditer.
- Accès VPS : `ssh vps-lafoncedalle` (clé déjà configurée) ; déploiement via `./deploy.sh "msg"` en local (build+commit+push+SSH deploy) ou `bash deploy.sh --vps` directement sur le VPS.
